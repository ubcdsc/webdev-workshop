import React from 'react';
import { Grid, Row, Col } from 'react-flexbox-grid';
import { StyledInput } from './StyledDashboard';

const Dashboard = () => {
  return (
    <Grid>
      <Row>
        <StyledInput />
      </Row>
    </Grid>
  )
}

export default Dashboard;